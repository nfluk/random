// checks if number is divisible by 7

function isDivisibleBy7(num) {
  return num % 7 === 0;
}

// checks if first number is divisible by second number

function isDivisibleBy(num1, num2) {
  return num1 % num2 === 0;
}
